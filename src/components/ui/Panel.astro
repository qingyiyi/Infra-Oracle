---
interface Props {
  as?: "div" | "section" | "article" | "aside";
  tone?: "default" | "accent" | "subtle";
  padding?: "md" | "lg";
  class?: string;
  [key: string]: unknown;
}

const {
  as = "div",
  tone = "default",
  padding = "md",
  class: className = "",
  ...attrs
} = Astro.props as Props;

const Tag = as;
---

<Tag {...attrs} class:list={["panel", `panel--${tone}`, `panel--${padding}`, className]}>
  <slot />
</Tag>
